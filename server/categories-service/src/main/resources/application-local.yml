spring:
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:.env
  datasource:
    url: jdbc:postgresql://localhost:5434/categories_db
    username: ${POSTGRES_CATEGORIES_USER}
    password: ${POSTGRES_CATEGORIES_PASSWORD}
  kafka:
    bootstrap-servers: localhost:9093