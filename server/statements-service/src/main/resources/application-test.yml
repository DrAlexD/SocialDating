spring:
  datasource:
    generate-unique-name: false
    name: statements-service-test
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: always
