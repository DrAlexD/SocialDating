management:
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,shutdown

spring:
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:.env
  kafka:
    bootstrap-servers: localhost:9093

services:
  gateway:
    host: localhost
  users:
    host: localhost
  categories:
    host: localhost
  defining-themes:
    host: localhost
  statements:
    host: localhost
